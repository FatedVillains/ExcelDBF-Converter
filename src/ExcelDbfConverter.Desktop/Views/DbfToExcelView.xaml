<UserControl x:Class="ExcelDbfConverter.Desktop.Views.DbfToExcelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="950">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,8,0">
            <TextBlock Text="DBF 转 Excel" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>

            <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="1. 选择 DBF 文件" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <DockPanel>
                        <Button Content="选择文件" Style="{StaticResource SecondaryButton}" DockPanel.Dock="Right" Command="{Binding BrowseCommand}"/>
                        <TextBox Text="{Binding SourcePath, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                    </DockPanel>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="2. 编码设置" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <DockPanel>
                        <TextBlock Text="当前编码：" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                        <ComboBox ItemsSource="{Binding Encodings}" SelectedItem="{Binding SelectedEncoding}" Width="200" Margin="8,0,0,0" HorizontalAlignment="Left"/>
                        <Button Content="重新加载" Style="{StaticResource SecondaryButton}" Command="{Binding ReloadCommand}" Margin="8,0,0,0" HorizontalAlignment="Left"/>
                    </DockPanel>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="3. 数据预览" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding PreviewInfo}" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,0,0,6"/>
                    <DataGrid ItemsSource="{Binding Fields}" AutoGenerateColumns="False" IsReadOnly="True" MaxHeight="160" Background="{StaticResource CardBrush}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="字段" Binding="{Binding Name}" Width="160"/>
                            <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="120"/>
                            <DataGridTextColumn Header="长度" Binding="{Binding Length}" Width="80"/>
                            <DataGridTextColumn Header="小数位" Binding="{Binding DecimalCount}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <DataGrid ItemsSource="{Binding PreviewTable}" AutoGenerateColumns="True" IsReadOnly="True" MaxHeight="220" Margin="0,8,0,0" Background="{StaticResource CardBrush}"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="4. Excel 配置" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Excel 格式：" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding ExcelFormats}" SelectedItem="{Binding SelectedExcelFormat}" Width="150" HorizontalAlignment="Left" Margin="4,0,0,0"/>

                        <TextBlock Grid.Row="1" Text="Sheet 名称：" VerticalAlignment="Center" Margin="0,8,0,0"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SheetName, UpdateSourceTrigger=PropertyChanged}" Width="300" HorizontalAlignment="Left" Margin="4,8,0,0"/>

                        <TextBlock Grid.Row="2" Text="每 Sheet 最大行数：" VerticalAlignment="Center" Margin="0,8,0,0"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding MaxRowsPerSheet, UpdateSourceTrigger=PropertyChanged}" Width="150" HorizontalAlignment="Left" Margin="4,8,0,0"/>

                        <CheckBox Grid.Row="3" Grid.Column="1" Content="包含已删除记录" IsChecked="{Binding IncludeDeleted}" Margin="4,8,0,0"/>

                        <TextBlock Grid.Row="4" Text="输出文件：" VerticalAlignment="Center" Margin="0,8,0,0"/>
                        <DockPanel Grid.Row="4" Grid.Column="1" Margin="4,8,0,0">
                            <Button Content="浏览" Style="{StaticResource SecondaryButton}" DockPanel.Dock="Right" Command="{Binding BrowseOutputCommand}"/>
                            <TextBox Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}"/>
                        </DockPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="开始转换" Style="{StaticResource PrimaryButton}" Command="{Binding ConvertCommand}" MinWidth="120"/>
                        <Button Content="取消" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}" MinWidth="80" Margin="8,0,0,0"/>
                    </StackPanel>

                    <ProgressBar Height="8" Maximum="100" Value="{Binding ProgressPercentage}" Margin="0,12,0,0"/>
                    <TextBlock Text="{Binding ProgressText}" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,4,0,0"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
