<UserControl x:Class="ExcelDbfConverter.Desktop.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="欢迎使用 ExcelDBF Converter" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Style="{StaticResource PrimaryButton}" Width="200" Height="120" Margin="0,0,16,0" Command="{Binding GoExcelToDbfCommand}">
                <StackPanel>
                    <TextBlock Text="Excel → DBF" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="将 Excel 转换为 DBF" FontSize="11" Opacity="0.85" Margin="0,6,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button Style="{StaticResource PrimaryButton}" Width="200" Height="120" Command="{Binding GoDbfToExcelCommand}">
                <StackPanel>
                    <TextBlock Text="DBF → Excel" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="将 DBF 转换为 Excel" FontSize="11" Opacity="0.85" Margin="0,6,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,24,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel>
                <TextBlock Text="最近转换记录" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Left"/>
                <Button Content="刷新" Style="{StaticResource SecondaryButton}" DockPanel.Dock="Right" Command="{Binding RefreshCommand}"/>
            </DockPanel>
            <DataGrid Grid.Row="1" Margin="0,8,0,0" ItemsSource="{Binding RecentHistory}" AutoGenerateColumns="False" IsReadOnly="True" Background="{StaticResource CardBrush}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding CreatedTime, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="160"/>
                    <DataGridTextColumn Header="文件" Binding="{Binding SourceFileName}" Width="220"/>
                    <DataGridTextColumn Header="类型" Binding="{Binding ConversionType}" Width="120"/>
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
