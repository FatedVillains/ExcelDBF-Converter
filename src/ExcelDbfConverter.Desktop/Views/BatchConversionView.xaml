<UserControl x:Class="ExcelDbfConverter.Desktop.Views.BatchConversionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="950">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,8,0">
            <TextBlock Text="批量转换" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16"/>

            <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="转换类型" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ComboBox ItemsSource="{Binding ConversionTypes}" SelectedItem="{Binding SelectedConversionType}" Width="200" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="源文件 / 源目录" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <DockPanel Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                            <Button Content="添加文件" Style="{StaticResource SecondaryButton}" Command="{Binding AddFilesCommand}"/>
                            <Button Content="选择目录" Style="{StaticResource SecondaryButton}" Command="{Binding BrowseSourceDirCommand}" Margin="8,0,0,0"/>
                            <Button Content="清空" Style="{StaticResource SecondaryButton}" Command="{Binding ClearFilesCommand}" Margin="8,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="源目录：" VerticalAlignment="Center"/>
                    </DockPanel>
                    <TextBox Text="{Binding SourceDirectory, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding SourceFiles}" MaxHeight="120"/>
                    <CheckBox Content="包含子目录" IsChecked="{Binding IncludeSubdirectories}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="输出与选项" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <DockPanel Margin="0,0,0,8">
                        <Button Content="选择输出目录" Style="{StaticResource SecondaryButton}" DockPanel.Dock="Right" Command="{Binding BrowseOutputDirCommand}"/>
                        <TextBox Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                    </DockPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="重名处理：" VerticalAlignment="Center"/>
                        <ComboBox ItemsSource="{Binding OverwriteModes}" SelectedItem="{Binding SelectedOverwriteMode}" Width="140" Margin="4,0,0,0"/>
                        <TextBlock Text="错误处理：" VerticalAlignment="Center" Margin="16,0,0,0"/>
                        <ComboBox ItemsSource="{Binding ErrorHandlingOptions}" SelectedItem="{Binding SelectedErrorHandling}" Width="140" Margin="4,0,0,0"/>
                        <TextBlock Text="Excel 格式：" VerticalAlignment="Center" Margin="16,0,0,0"/>
                        <ComboBox ItemsSource="{Binding ExcelFormats}" SelectedItem="{Binding SelectedExcelFormat}" Width="100" Margin="4,0,0,0"/>
                        <TextBlock Text="编码：" VerticalAlignment="Center" Margin="16,0,0,0"/>
                        <ComboBox ItemsSource="{Binding Encodings}" SelectedItem="{Binding SelectedEncoding}" Width="100" Margin="4,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="开始批量转换" Style="{StaticResource PrimaryButton}" Command="{Binding StartCommand}" MinWidth="120"/>
                        <Button Content="取消" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}" MinWidth="80" Margin="8,0,0,0"/>
                    </StackPanel>
                    <ProgressBar Height="8" Maximum="100" Value="{Binding ProgressPercentage}" Margin="0,12,0,0"/>
                    <TextBlock Text="{Binding ProgressText}" Foreground="{StaticResource MutedTextBrush}" FontSize="11" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource CardBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <DockPanel Margin="0,0,0,8">
                        <Button Content="清空结果" Style="{StaticResource SecondaryButton}" DockPanel.Dock="Right" Command="{Binding ClearResultsCommand}"/>
                        <TextBlock Text="转换结果" FontWeight="SemiBold"/>
                    </DockPanel>
                    <TextBlock Text="{Binding Summary}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <DataGrid ItemsSource="{Binding Results}" AutoGenerateColumns="False" IsReadOnly="True" MaxHeight="240" Background="{StaticResource CardBrush}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="文件名称" Binding="{Binding FileName}" Width="200"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="90"/>
                            <DataGridTextColumn Header="信息" Binding="{Binding Message}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
