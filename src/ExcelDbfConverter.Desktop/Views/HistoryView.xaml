<UserControl x:Class="ExcelDbfConverter.Desktop.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="950">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Margin="0,0,0,8">
            <TextBlock Text="转换历史" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}" DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="刷新" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}"/>
                <Button Content="清空历史" Style="{StaticResource SecondaryButton}" Command="{Binding ClearCommand}" Margin="8,0,0,0"/>
            </StackPanel>
        </DockPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Entries}" AutoGenerateColumns="False" IsReadOnly="True" Background="{StaticResource CardBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding CreatedTime, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="150"/>
                <DataGridTextColumn Header="源文件" Binding="{Binding SourceFileName}" Width="180"/>
                <DataGridTextColumn Header="目标文件" Binding="{Binding TargetFileName}" Width="180"/>
                <DataGridTextColumn Header="类型" Binding="{Binding ConversionType}" Width="110"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="90"/>
                <DataGridTextColumn Header="行数" Binding="{Binding SuccessRows}" Width="80"/>
                <DataGridTextColumn Header="耗时(ms)" Binding="{Binding ElapsedMilliseconds}" Width="90"/>
                <DataGridTemplateColumn Header="操作" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="重新打开" Style="{StaticResource SecondaryButton}" FontSize="11" Padding="6,2"
                                        Command="{Binding DataContext.ReopenFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="打开目录" Style="{StaticResource SecondaryButton}" FontSize="11" Padding="6,2" Margin="4,0,0,0"
                                        Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="删除" Style="{StaticResource SecondaryButton}" FontSize="11" Padding="6,2" Margin="4,0,0,0"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
